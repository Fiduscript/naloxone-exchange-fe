.pt-3.pb-3(*ngIf="show")
  .d-flex.justify-content-end
    button.btn.dismiss(
        *ngIf="showClose"
        "(click)"="dismiss()"
        title="dismiss"
        type="button"
        aria-label="Close") x
  .row.d-flex.justify-content-around
    .col-sm-12.col-md-5.pr-0
      h5.mb-0 Subscribe for update! 
      small.form-text We will never share this information with third parties.
    .col-sm-12.col-md-7.pl-0
      form.mt-md-1("[formGroup]"="subscribeForm" "(ngSubmit)"="subscribe()")
        .form-row.d-flex.justify-content-end
          .form-group.col-sm-12.col-md-6.col-lg-6
            input.form-control(
                type="email"
                placeholder="Ex. john.doe@email.com"
                name="email"
                "[formControl]"="subscribeForm.controls['email']"
                required)
            small.form-text.text-danger(*ngIf="subscribeForm.touched && subscribeForm.get('email').invalid") 
              | A valid email address is required.
          .form-group.col-sm-12.col-md-3.col-lg-3
            select.form-control.mr-lg-4(
                name="state"
                "[formControl]"="subscribeForm.controls['state']"
                required)
                option(value="" selected disabled) State
                option(*ngFor="let state of getStates()" "[value]"="state.name") {{state.name}}
            small.form-text.text-danger(*ngIf="subscribeForm.touched && subscribeForm.get('state').invalid") State is required.

          .form-group.btn-group
            button.btn.btn-primary(
                "[disabled]"="!subscribeForm.valid"
                title="subscribe for updates"
                type="submit") Subscribe

