.row.mt-2
  .col-lg-5
    img.-align-left.img-pharmacy.mb-4(src="{{pharmacy.image}}")
  .col-lg-7
    h2 {{pharmacy.name}}
    h4 {{pharmacy.address}}
    p {{pharmacy.description}}
    h4(*ngIf="pharmacy.hours.length") Hours
    p(*ngFor="let hour of pharmacy.hours") {{hour}}
    h4 Contact Info
    p(*ngIf="pharmacy.email") Email: 
      a(href="mailto:{{pharmacy.email}}") {{pharmacy.email}}
    p(*ngIf="pharmacy.phone") Phone: 
      a(href="tel:{{pharmacy.phone}}") {{pharmacy.phone}}
    p(*ngIf="pharmacy.homepage") Visit 
      a(href="{{pharmacy.homepage}}" target="_blank") {{pharmacy.name}}
      span 's website
